# Configuration

Use:
- built-in defaults
- CLI flags (`ui-test play --help`, `ui-test record --help`, `ui-test improve --help`)

For first-run setup and browser provisioning:

```bash
ui-test setup
```
